name: Build

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

# ---------------------------------------------------------
# GLOBAL CACHE KEY
# ---------------------------------------------------------
env:
  HAXE_VERSION: 4.3.4
  LIME_VERSION: latest

# ---------------------------------------------------------
# REUSABLE STEPS
# ---------------------------------------------------------
jobs:
  # ---------------------------------------------------------
  # WINDOWS
  # ---------------------------------------------------------
  buildWindows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4.1.7

      - uses: actions/cache@v4
        with:
          path: |
            C:/haxelib
          key: haxe-windows-${{ hashFiles('hmm.json') }}

      - uses: krdlab/setup-haxe@master
        with:
          haxe-version: ${{ env.HAXE_VERSION }}

      - name: Fix PATH
        run: echo "C:\HaxeToolkit\haxe" >> $env:GITHUB_PATH

      - name: Install Haxelib Dependencies
        run: |
          haxelib setup C:/haxelib
          haxelib git polymod https://github.com/froncekmatthew173-bot/polymod-dnb.git main
          haxelib install hmm
          haxelib run hmm setup
          haxelib run hmm install
          haxelib install hxvlc 2.2.6
          haxelib run lime rebuild windows
          haxelib list

      - name: Create Version Tag
        run: echo "${{ github.run_id }}" > VERSION

      - name: Compile Windows
        run: haxelib run lime build windows --app-version="4.0.0-${{ github.run_id }}"

      - uses: actions/upload-artifact@v4
        with:
          name: windowsBuild
          path: export/release/windows/bin
