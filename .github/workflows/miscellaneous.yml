name: Miscellaneous CI
on:
  workflow_dispatch:
  workflow_call:

env:
  HAXE_VERSION: 4.3.7

jobs:
  miscellaneous:
    runs-on: windows-2025
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Haxe
        uses: krdlab/setup-haxe@master
        with:
          haxe-version: ${{ env.HAXE_VERSION }}
      - name: Set HAXEPATH
        run: echo "HAXEPATH=$HAXE_STD_PATH/.." >> $GITHUB_ENV
      - name: Install Haxe dependencies
        run: |
          haxelib git hxcpp https://github.com/ShadowEngineTeam/hxcpp --quiet
          haxelib git format https://github.com/ShadowEngineTeam/format --quiet
          haxelib git hxp https://github.com/ShadowEngineTeam/hxp --quiet
          haxelib git openfl https://github.com/FunkinCrew/openfl 5399946b68fd98f345d6b3e52b95ed6b78bf427c --quiet
          haxelib git svg https://github.com/openfl/svg d74c6ef5ee2e8656a69a36c79e67a09086c6a9a4 --quiet
      - name: Enable HXCPP compile cache
        run: echo "HXCPP_COMPILE_CACHE=~/.hxcpp" >> $GITHUB_ENV
      - name: Configure Lime
        run: haxelib dev lime ${{ github.workspace }}
      - name: Rebuild Miscellaneous
        run: |
          cd tools
          haxe svg.hxml
          haxe run.hxml
          haxe tools.hxml
          haxe alias.hxml
          cp obj/lime/AliasScript.exe ../templates/bin/lime.exe
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Miscellaneous
          path: |
            tools/tools.n
            run.n
            svg.n
            templates/bin/lime.exe
          if-no-files-found: error
